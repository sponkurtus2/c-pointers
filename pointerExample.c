#include <stdio.h>

void printAge(int *age) { printf("You are %d years old\n", *age); }

int main() {

  // int age = 19;
  // int *pAge = &age;

  // printf("Value of age %d\n", age);
  // printf("Value at stored address %d\n", *pAge);
  // printf("--------------------------------\n");
  // printf("Address of age %p\n", &age);
  // printf("Value of pAge %p\n", pAge);
  // printf("--------------------------------\n");
  // printf("Size of normal age: %lu bytes \n", sizeof(age));
  // printf("Size of pointer to age (pAge): %lu \n", sizeof(pAge));
  // printAge(pAge);
  return 0;
}
